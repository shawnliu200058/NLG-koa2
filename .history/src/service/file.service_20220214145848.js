const promisePool = require('../app/database')

class FileService {
  async createCategoryIcon(filename, mimetypr, categoryId) {
    const statement = `INSERT INTO  (filename, mimetype, category_id) `
  }
}